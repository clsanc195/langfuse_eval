# ─── Secrets — never commit ─────────────────────────────
.env
.env.*
!.env.example
!.env.sample
!.env.template

*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
*.jks
*.keystore
*.gpg

# Cloud credentials
gcp-*.json
service-account*.json
service_account*.json
*-credentials.json
credentials.json
client_secret*.json

# Generic secret names
secrets/
secret.*
*.secret
*secrets.yml
*secrets.yaml
auth.json
token.json
*.token
.netrc

# ─── Local-only ─────────────────────────────────────────
# Cloned upstream Langfuse repo (re-cloned by setup.sh)
langfuse/

# Large media (recordings can be many MB — host elsewhere if sharing)
*.mov
*.mp4

# ─── Python ─────────────────────────────────────────────
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ─── Jupyter ────────────────────────────────────────────
.ipynb_checkpoints/

# ─── OS ─────────────────────────────────────────────────
.DS_Store
